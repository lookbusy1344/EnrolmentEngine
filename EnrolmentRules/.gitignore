BenchmarkDotNet.Artifacts/

# Vue ClientApp: reproducible from source + pnpm-lock.yaml, not checked in (see
# docs/plans/2026-07-16-vue-enrolment-app-plan.md).
src/EnrolmentRules.Web/ClientApp/node_modules/
src/EnrolmentRules.Web/ClientApp/dist/
src/EnrolmentRules.Web/ClientApp/**/*.tsbuildinfo
src/EnrolmentRules.Web/wwwroot/app/
src/EnrolmentRules.Web/wwwroot/lib/

# Generated by scripts/deploy-cloudrun.sh to carry the commit hash into a build that
# cannot reach git (see Directory.Build.props' StampGitCommit). Deliberately NOT in
# .gcloudignore/.dockerignore — the build needs it. The script deletes it on exit.
.sourcerevision

# duplicated just to ensure ignore rules are self-contained in project
**/wwwroot/lib/
